id: KI-5
title: decodeSingleUnicodeEscape length guard too small — OOB read + panic on a truncated \uXX escape
affected_requirements:
- SYS-REQ-060
- SYS-REQ-014
- SYS-REQ-038
affected_api: decodeSingleUnicodeEscape
severity: high
severity_basis: reproducer
risk: availability
cve_surface: likely
cvss: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:H
attacker_input: a JSON string value whose `\u` escape is truncated at end-of-buffer, e.g. {"k":"\u00"}
environment_note: Reachable via any Get*/ParseString/Unescape call on attacker-controlled JSON that
  contains a `\u` escape truncated near the end of the input buffer.
description: 'decodeSingleUnicodeEscape (escape.go) decodes a `\uXXXX` escape by reading four hex digits
  at in[2..5] after a minimum-length guard. On the proof-demo showcase branch that guard was weakened
  from `if len(in) < 6` to `if len(in) < 4` (escape.go:88), so a truncated escape such as `\u00` — only
  four bytes available — passes the check and the decoder indexes in[4] and in[5] one and two bytes past
  the end of the backing slice. This is a textbook CWE-125 Out-of-bounds Read: the boundary condition
  (fewer than the six bytes the escape requires) is no longer rejected before the indexed reads. The Go
  runtime turns the out-of-bounds index into an index-out-of-range panic, so any caller parsing the input
  is crashed — a denial of service. Intentional demo defect on the proof-demo showcase branch; never
  shipped.'
customer_impact: A consumer parsing attacker-controlled JSON can be crashed (denial of service) by a single
  truncated `\u` escape — one malformed byte sequence aborts the request/goroutine handling it. Intentional
  demo defect on the proof-demo showcase branch; never shipped in a released jsonparser.
mitigation: Recover from parser panics at the call boundary, cap incoming JSON size, and validate or reject
  untrusted input before parsing so a single malformed escape cannot abort the handler.
remediation: Restore the 6-byte minimum-length guard in decodeSingleUnicodeEscape (escape.go:88, `if len(in)
  < 4` -> `if len(in) < 6`) so a truncated `\uXX` returns (RuneError, false) before any indexed read.
  Closes CWE-125 Out-of-bounds Read; cf. RFC 8259 section 7 on `\uXXXX` escapes.
reproduction_steps:
- Check out the proof-demo showcase branch and build the package (go build ./...).
- 'Call jsonparser.GetString([]byte(`{"k":"\u00"}`), "k") — or run: go test -run TestDemoKI5 -count=1
  .'
- Observe an index-out-of-range panic from decodeSingleUnicodeEscape instead of a clean MalformedStringEscapeError.
- Revert escape.go:88 back to `if len(in) < 6` and re-run; the reproducer now returns the malformed-escape
  error.
evidence_manifests:
- proof/evidence/KI-5-reproducer.yaml
- proof_demo_reproducers_test.go:TestDemoKI5_TruncatedUnicodeEscapeOOB
poc_quality:
  real_infrastructure: true
  quantitative_impact: false
  control_test: true
  untrusted_attacker: false
  current_deployment_relevance: true
  honest_severity: true
  attacker_profitability: false
  reproducible: true
  no_inflation: true
  operational_path_verified: true
  reachability_verified: false
  not_applicable:
    reachability_verified: "local-behavioral robustness defect in a parsing LIBRARY: there is no ingress access-control or authentication layer for an exploit to traverse, so end-to-end reachability past a guard is definitionally N/A. The triggering bytes are ordinary caller-supplied input to the public API (Unescape/Get*), not an exploit crossing a trust boundary."
  notes: |
    real_infrastructure/reproducible: executed in-repo reproducer `go test -run TestDemoKI5_TruncatedUnicodeEscapeOOB -count=1 .` drives the real public API (Unescape) AND calls the mutated internal symbol decodeSingleUnicodeEscape directly (no mock/stub); single command, <1s (rules 1 & 8).
    control_test/operational_path_verified: the reproducer binds a positive CONTROL asserting a well-formed `A` escape decodes to "A" and does NOT trigger the OOB — the safe/intended path is exercised and stays clean (rules 3 & 10).
    honest_severity/no_inflation: severity=high basis=reproducer (availability); the PoC name matches the demonstrated impact (CWE-125 OOB read -> index-out-of-range panic -> DoS on a truncated `\u` escape); no external submission, grade not inflated (rules 6 & 9).
    current_deployment_relevance: KI status=open — the weakened `len(in) < 4` guard is present in the current proof-demo tree and reachable through the documented public API (rule 5).
    quantitative_impact/untrusted_attacker/attacker_profitability: N/A — jsonparser is a parsing library, not a Web3/DeFi or value-custody service; these three rules are Web3-only and auto-skipped on a non-Web3 project (rules 2, 4, 7).
    reachability_verified: N/A (declared above) — local-behavioral correctness/robustness defect, no trust-boundary crossing (rule 11).
commands:
- go test ./... -count=1
reproducer_command: go test -run TestDemoKI5 -count=1 .
tripwire_mutation: escape.go:88 — the length guard was mutated from `if len(in) < 6` to `if len(in) <
  4`. With only 4 bytes available, decodeSingleUnicodeEscape reads in[4] and in[5] out of bounds. Reverting
  `< 4` back to `< 6` flips the TestDemoKI5 reproducer from PANIC back to a clean MalformedStringEscapeError.
sanitizer: go-runtime-panic:index-out-of-range
minimization_status: minimized
introduced_in: inception
disclosure: public
owner: human:buger
review_date: '2026-08-03'
release_disposition: open
status: open
history:
- at: '2026-08-03T09:00:00Z'
  by: human:buger
  action: created
- at: '2026-08-03T12:00:00Z'
  by: human:buger
  action: edit
  detail: 'enriched metadata: cvss, security classification, reproduction steps, evidence'
