id: KI-10
title: arrayEachConfig empty-array sentinel checks for '}' instead of ']' — a valid `[]` is rejected as
  malformed
affected_requirements:
- SYS-REQ-028
affected_api: arrayEachConfig
severity: medium
severity_basis: risk
risk: correctness
cve_surface: none
cvss: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:L/A:N
attacker_input: a well-formed empty JSON array, []
environment_note: Reachable via ArrayEach/ArrayEachErr on any empty array value; every empty-array input
  is affected, so it surfaces immediately on valid data rather than needing a crafted payload.
description: 'arrayEachConfig (parser.go) fast-paths an empty array by checking whether the first significant
  byte is the closing bracket. On the proof-demo showcase branch the sentinel was mutated from `if data[offset]
  == '']''` to `if data[offset] == ''}''` (parser.go:1763), so a valid empty array `[]` no longer matches
  the empty-array sentinel; the loop then runs against the closing bracket and returns MalformedArrayError.
  This is a CWE-20 Improper Input Validation: well-formed input is rejected because the sentinel compares
  against the wrong delimiter. Intentional demo defect on the proof-demo showcase branch; never shipped.'
customer_impact: Callers iterating arrays with ArrayEach get a spurious MalformedArrayError on the perfectly
  valid empty array `[]`, breaking otherwise-correct code paths that handle empty collections. Intentional
  demo defect on the proof-demo showcase branch; never shipped.
mitigation: Special-case/short-circuit empty arrays in caller code, or treat MalformedArrayError on known
  array fields as "empty", until the sentinel is corrected.
remediation: Restore the `']'` closing-bracket comparison in the empty-array fast path of arrayEachConfig
  (parser.go:1763, `data[offset] == '}'` -> `data[offset] == ']'`) so `[]` iterates zero times with a
  nil error. Closes CWE-20 Improper Input Validation.
reproduction_steps:
- Check out the proof-demo showcase branch and build the package.
- 'Call jsonparser.ArrayEach on the document `[]` — or run: go test -run TestDemoKI10 -count=1 .'
- Observe MalformedArrayError instead of a clean zero-iteration success.
evidence_manifests:
- proof/evidence/KI-10-reproducer.yaml
- proof_demo_reproducers_test.go:TestDemoKI10_EmptyArrayEach
poc_quality:
  real_infrastructure: true
  quantitative_impact: false
  control_test: true
  untrusted_attacker: false
  current_deployment_relevance: true
  honest_severity: true
  attacker_profitability: false
  reproducible: true
  no_inflation: true
  operational_path_verified: true
  reachability_verified: false
  not_applicable:
    reachability_verified: "local-behavioral correctness defect in a parsing LIBRARY: there is no ingress access-control or authentication layer for an exploit to traverse, so end-to-end reachability past a guard is definitionally N/A. The triggering input is an ordinary caller-supplied empty array to the public API (ArrayEach), not an exploit crossing a trust boundary."
  notes: |
    real_infrastructure/reproducible: executed in-repo reproducer `go test -run TestDemoKI10_EmptyArrayEach -count=1 .` drives the real public API (ArrayEach, which routes through the mutated arrayEachConfig) with no mock/stub; single command, <1s (rules 1 & 8).
    control_test/operational_path_verified: the reproducer binds a positive CONTROL asserting a NON-empty array `[1,2]` iterates exactly twice with a nil error and does NOT trigger the defect — the safe/intended path stays correct (rules 3 & 10).
    honest_severity/no_inflation: severity=medium basis=risk (correctness); the PoC name matches the demonstrated impact (CWE-20 improper input validation: the empty-array sentinel compares against '}' instead of ']', so a valid `[]` is wrongly rejected as MalformedArrayError); no external submission, grade not inflated (rules 6 & 9).
    current_deployment_relevance: KI status=open — the mutated sentinel is present in the current proof-demo tree and reachable through the documented public API (rule 5).
    quantitative_impact/untrusted_attacker/attacker_profitability: N/A — jsonparser is a parsing library, not a Web3/DeFi or value-custody service; these three rules are Web3-only and auto-skipped on a non-Web3 project (rules 2, 4, 7).
    reachability_verified: N/A (declared above) — local-behavioral correctness defect, no trust-boundary crossing (rule 11).
commands:
- go test ./... -count=1
reproducer_command: go test -run TestDemoKI10 -count=1 .
tripwire_mutation: parser.go:1763 — the empty-array sentinel was mutated from `if data[offset] == ']'`
  to `if data[offset] == '}'`. An empty array no longer matches the sentinel, so the loop runs against
  the closing bracket and returns MalformedArrayError. Reverting '}' back to ']' flips the TestDemoKI10
  reproducer from MalformedArrayError back to a clean zero-iteration success.
sanitizer: audit:hazard-sweep
minimization_status: minimized
introduced_in: inception
disclosure: public
owner: human:buger
review_date: '2026-08-03'
release_disposition: open
status: open
history:
- at: '2026-08-03T09:00:00Z'
  by: human:buger
  action: created
- at: '2026-08-03T12:00:00Z'
  by: human:buger
  action: edit
  detail: 'enriched metadata: cvss, security classification, reproduction steps, evidence'
